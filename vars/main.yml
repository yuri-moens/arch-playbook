---
user:
  name: ymo
  group: ymo
  groups:
    - wheel
  shell: /usr/bin/zsh

sudoers_group: wheel

gpu_packages:
  - nvidia
  - nvidia-settings
  - lib32-nvidia-utils

base_packages:
  - htop
  - git
